@model IEnumerable<BugTracker.Models.Notification>

@{
    ViewBag.Title = "Index";
}


<h2>Notifications</h2>


<table class="table">
    <tr>
        <th>
            From
        </th>
        <th>
            To
        </th>
        <th>
            Date Sent
        </th>
        <th>
            Notification
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BeenRead)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(m => item.User1.ASPUserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OnDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Notification1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.ASPUserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BeenRead)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>*@
    </tr>
}

</table>

<p>
    @Html.ActionLink("Create New", "Create")
</p>